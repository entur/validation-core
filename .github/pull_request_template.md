## Description

<Describe the purpose of this change in one or two sentences.>

Fixes ETU-####.

## Checklist

- [ ] Correct Jira ticket key in the PR title (`feat(ETU-####): ...` or `ETU-####: ...`)
- [ ] `.proto` changes reviewed for wire compatibility (`buf breaking` passes)
- [ ] Regenerated `specs/*.yaml` committed alongside the `.proto` change
- [ ] Documentation updated if needed
- [ ] Consumers informed of breaking changes
