# Local-build default only. Publishing is tag-triggered - CD overrides this with the version
# from the pushed tag. See "Versioning and publishing" in README.md.
# -SNAPSHOT (not a plain release number) so a local `publishToMavenLocal` can
# never collide with or shadow a real vX.Y.Z tag published to JFrog.
version=0.0.1-SNAPSHOT
